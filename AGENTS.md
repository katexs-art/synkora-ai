# AGENTS.md

Katexs platform repo — read `CLAUDE.md` at the repo root FIRST for the full context
(architecture, theming system, widget system, deploy rules, and Katexs-custom code).
Key rules: production is live; never run dev mode on port 3005; never leave two web
servers running; keep the light cream theme unless the owner asks otherwise; widget JS
global stays `SynkoraWidget`; branding lives in platform_settings + `katexs-accent.css`.
